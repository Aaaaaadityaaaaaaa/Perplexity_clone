name: spencer
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter

  # UI & Fonts
  cupertino_icons: ^1.0.8
  google_fonts: ^6.2.1

  # Core functionality
  web_socket_client: ^0.2.0
  flutter_markdown: ^0.7.7
  skeletonizer: ^1.4.3
  url_launcher: ^6.2.5
  http: ^1.3.0 # ✅ For fetching news
  html_unescape: ^2.0.0 # ✅ For decoding HTML entities in titles

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
